# Конфигурация

Здесь собраны решения, которые стоит принять осознанно перед продакшеном. Все ключи, которые
понимает пакет, со значениями по умолчанию — в
[`config/example_config.yaml`](../../config/example_config.yaml).

## Два слоя, без пересечений

| | Отвечает за | Задаётся |
|---|---|---|
| OpenTelemetry SDK и экспорт | endpoint, протокол, заголовки, sampler, экспортёры, размеры очередей, интервалы, детекторы ресурса | переменные `OTEL_*` / `OTEL_PHP_*` |
| Пакет | что инструментируется, что записывается, бюджет flush на границе | `config/packages/open_telemetry.yaml` |

Ни один ключ пакета не дублирует переменную `OTEL_*`. Там, где пакет всё-таки принимает id
сервиса — sampler, экспортёр, провайдер, — это не дублирование: переменная выбирает из
реализаций, которые SDK уже знает, а id передаёт конвейеру ту, которой он не знает.

## 1. Экспортируйте в локальный коллектор

Рекомендуемая схема — [agent pattern] OpenTelemetry Collector: приложение → коллектор на том же
хосте, в sidecar или в локальной сети → удалённый бэкенд.

```text
PHP-воркер
   │ короткая, ограниченная передача по OTLP
   ▼
Collector / Alloy, локально
   │ очереди, ретраи, учётные данные, маршрутизация
   ▼
удалённые бэкенды
```

Что это даёт:

- задержка OTLP для приложения маленькая и предсказуемая;
- удалённые ретраи и очереди происходят вне PHP, где они не блокируют воркер;
- учётные данные бэкендов лежат в одном месте, а не в каждом приложении;
- недоступность бэкенда — проблема коллектора, а не запроса.

Экспорт напрямую в удалённый SaaS-endpoint работает, но таймауты поднимайте осознанно. Telemetry
Weaver ограничивает стандартный транспорт, но не сокращает сетевой round trip, и это не надёжная
очередь: телеметрия, не поместившаяся в бюджет, отбрасывается намеренно.

[agent pattern]: https://opentelemetry.io/docs/collector/deploy/agent/

## 2. Протокол

**HTTP/protobuf** — по умолчанию и проще всего:

```dotenv
OTEL_EXPORTER_OTLP_PROTOCOL=http/protobuf
OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318
```

Нужны `open-telemetry/exporter-otlp`, PSR-18-клиент и PSR-17-фабрика.

**gRPC** — когда окружение уже на нём:

```bash
composer require open-telemetry/transport-grpc   # требует ext-grpc
```

```dotenv
OTEL_EXPORTER_OTLP_PROTOCOL=grpc
OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4317
```

Бюджет действует одинаково для обоих. Проверьте путь через gRPC в настоящем образе развёртывания
— особенно TLS и восстановление после перезапуска коллектора.

## 3. Два таймаута, и какой из них какой

```dotenv
OTEL_EXPORTER_OTLP_TIMEOUT=500
```

```yaml
open_telemetry:
    sdk:
        export:
            flush_timeout_ms: 1000
```

`OTEL_EXPORTER_OTLP_TIMEOUT` — потолок одной отправки по OTLP. `flush_timeout_ms` — общий дедлайн
одной границы выполнения (конец запроса, завершившаяся команда, воркер между сообщениями),
покрывающий все сигналы, которые нужно отправить. Каждая отправка получает меньшее из двух.

Когда коллекторов несколько, дедлайн границы делится между ними, а не умножается на их число.
См. [Устойчивость экспорта](export-resilience.md).

Локальному коллектору комфортны 250–500 мс на отправку и 500–1000 мс на границу. Дальше —
измеряйте. Удалённый коллектор, которому нужны целые секунды, лучше придвинуть к приложению, чем
выдавать ему всё большую долю каждого запроса.

## 4. Держите ретраи вне PHP

```yaml
open_telemetry:
    sdk:
        export:
            max_retries: 0    # по умолчанию
```

Стандартный PHP-транспорт повторяет попытки синхронно: каждая спит в вызывающем процессе. Когда
коллектор лежит, три ретрая превращают одну неудачную отправку в несколько таймаутов плюс
backoff — внутри воркера, который должен обслуживать трафик. Потерять эту телеметрию дешевле.

Ретраи и очередь отправки настраиваются на коллекторе, где они асинхронны.

## 5. Трейсы: sampler, процессор, очередь

```dotenv
OTEL_TRACES_SAMPLER=parentbased_traceidratio
OTEL_TRACES_SAMPLER_ARG=0.1

OTEL_PHP_TRACES_PROCESSOR=batch
OTEL_BSP_SCHEDULE_DELAY=1000
OTEL_BSP_MAX_QUEUE_SIZE=2048
OTEL_BSP_MAX_EXPORT_BATCH_SIZE=512
```

Сэмплирование — первый рычаг, а не последний. Решите его до того, как ужимать очереди под объём.
`parentbased_traceidratio` сохраняет решение, пришедшее снаружи, и сэмплирует новые корневые
трейсы с заданной долей; `always_on` уместен в локальной разработке.

Пакет строит batch-процессор с выключенным авто-flush: `span->end()` только ставит спан в
очередь, а очередь опустошается на границах выполнения. Если очередь заполнилась раньше
следующей границы, спаны отбрасываются, а не экспортируются на месте — ограниченная потеря
вместо неограниченной очереди или сетевого ожидания внутри бизнес-кода. Размер очереди считайте
**на один воркер**.

`OTEL_PHP_TRACES_PROCESSOR=simple` — сознательный отказ от всего этого: экспорт происходит в
момент завершения спана, возвращая задержку коллектора на путь запроса.

Когда решение о сэмплировании не выражается ни одним sampler'ом из SDK — никогда этот healthcheck,
всегда оформление заказа, пять процентов остального, — настройте свой сервис и сохраните всё, что
пакет строит вокруг него:

```yaml
open_telemetry:
    sdk:
        traces:
            sampler: App\Telemetry\PerRouteSampler
```

См. [Кастомизацию SDK](sdk-customization.md).

## 6. Метрики: частота и temporality

```dotenv
OTEL_METRIC_EXPORT_INTERVAL=15000
```

Метрики экспортируются на границах выполнения. Каждый экспорт — блокирующая отправка, поэтому
интервал и есть то, что не даёт нагруженному приложению экспортировать на каждой границе.
`metrics.flush_interval_ms: null` следует за `OTEL_METRIC_EXPORT_INTERVAL` — за тем же, на чём
экспортёр работал бы и сам; задавайте значение, только чтобы флашить *реже*.

Temporality следует выбранному предпочтению SDK, но по инструментам, а не одной настройкой на
всех:

```dotenv
OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE=delta
```

При `delta` счётчики и гистограммы дельтовые, а up-down-счётчики и состояние остаются
кумулятивными: у них смысл — текущее значение, и потребитель не должен проигрывать все дельты с
момента старта процесса, чтобы узнать, сколько памяти занимает воркер.

Если бэкенду дельты не нужны, оставьте значение SDK по умолчанию.

## 7. Логи: корреляция и экспорт — разные вещи

Корреляция добавляет `trace_id` и `span_id` в записи лога, чтобы строку можно было найти из
трейса, а трейс — из строки. Ниже по конвейеру она ничего не стоит и включена по умолчанию.

Экспорт самих записей по OTLP — это вторая точка назначения для каждой строки лога, и он
выключен:

```yaml
open_telemetry:
    logs:
        correlation:
            enabled: true
        export:
            enabled: false
```

Оставить выключенным — правильный ответ, когда логи и так доезжают до Loki, Elastic или journald.
Чтобы включить, нужны `symfony/monolog-bundle` (handler встраивается в собственный стек Monolog) и
названный экспортёр:

```dotenv
OTEL_LOGS_EXPORTER=otlp
OTEL_BLRP_SCHEDULE_DELAY=1000
OTEL_BLRP_MAX_QUEUE_SIZE=2048
OTEL_BLRP_MAX_EXPORT_BATCH_SIZE=512
```

```yaml
open_telemetry:
    logs:
        export:
            enabled: true
            level: info                  # не debug: отгружать каждую debug-строку — решение про трафик
            excluded_channels: []
```

Экспорт логов устроен так же, как трейсы — очередь и опустошение на границе, — поэтому запись
строки лога не ждёт коллектор.

Канал `open_telemetry` не экспортируется никогда, что бы ни было в этом списке: запись о
неудачном экспорте, экспортированная наружу, снова наполняет очередь, из-за которой сбой и
произошёл.

## 8. Один endpoint или несколько

Один локальный коллектор — простой случай: для OTLP/HTTP SDK сам выводит `/v1/traces`,
`/v1/metrics` и `/v1/logs` из базового адреса:

```dotenv
OTEL_EXPORTER_OTLP_ENDPOINT=http://127.0.0.1:4318
```

Endpoint'ы по сигналам используются ровно как написано, поэтому путь сигнала нужно указывать:

```dotenv
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://tempo:4318/v1/traces
OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://mimir:4318/v1/metrics
OTEL_EXPORTER_OTLP_LOGS_ENDPOINT=http://loki:4318/v1/logs
```

Три хоста — три независимых домена отказа, и бюджет flush так их и рассматривает. Три пути на
одном хосте — один домен.

## 9. Заголовки и аутентификация

```dotenv
OTEL_EXPORTER_OTLP_HEADERS=Authorization=Bearer%20token
```

`sdk.exporter_otlp_headers` накладывается поверх этой переменной для OTLP-транспортов пакета:
записи отсюда выигрывают при совпадении, а значение `null` убирает заголовок, заданный
переменной. Собственное семейство транспортов отвечает за свою аутентификацию само и этого
слияния не видит.

## 10. Идентичность воркера и runtime mode

Переживает ли конвейер запрос — не настройка пакета: это следует из
`kernel.runtime_mode.web` / `kernel.runtime_mode.worker` Symfony, вычисленных из
`APP_RUNTIME_MODE`. Не выводите это из `PHP_SAPI`.

| Рантайм | Режим | На границе |
|---|---|---|
| PHP-FPM, процесс на запрос | `web=1&worker=0` | flush и завершение конвейера на terminate |
| Общий HTTP-воркер (FrankenPHP, RoadRunner) | `worker=1` | flush на каждый запрос, shutdown при выходе процесса |
| Воркер, пересобирающий ядро на каждый запрос | `worker=2` | завершение конвейера на каждый запрос, идентичность воркера сохраняется |
| Console, Messenger | — | flush на команду или сообщение, shutdown при выходе процесса |

FrankenPHP работает с этой моделью из коробки. Для собственной интеграции с RoadRunner или своего
bootstrap проверьте итоговые параметры: общий HTTP-воркер должен выглядеть как `web=1&worker=1`,
что можно задать явно:

```dotenv
APP_RUNTIME_MODE=web=1&worker=1
```

Долгоживущие воркеры получают стабильный `service.instance.id` от детектора SDK — именно он
отличает два воркера одного сервиса. Задавайте его руками, только если можете гарантировать
уникальность на каждый одновременно работающий воркер: одинаковое имя хоста на всех процессах
делает их ряды неразличимыми, что хуже сгенерированного значения.

## 11. Request-метрики под FPM

Конвейер «процесс на запрос» создаёт новый MeterProvider на каждый запрос, поэтому кумулятивные
счётчики постоянно начинаются заново, а состояние воркера не значит ничего. Request-метрики
поэтому выключены:

```yaml
open_telemetry:
    runtime:
        request_metrics:
            mode: disabled    # по умолчанию
```

Включайте, только держа в голове модель на принимающей стороне:

```yaml
open_telemetry:
    runtime:
        request_metrics:
            mode: delta
```

В режиме `delta` счётчики и гистограммы экспортируются с дельтовой temporality, выбранной до
агрегации; всё остальное остаётся кумулятивным и описывает один этот запрос. Требуется:

- ресурс, различающий писателей — `service.instance.id` либо `process.pid` вместе с атрибутами
  хоста или контейнера. Один общий `service.instance.id=${HOSTNAME}` на всех детей FPM — ровно
  тот случай, на котором это ломается;
- детекторы, которые эту идентичность действительно дают. Ограничение `OTEL_PHP_DETECTORS`
  значением `env` её убирает. Безопасный явный набор — `env,host,process,process_runtime,sdk`;
  значение SDK по умолчанию `all` тоже подходит;
- конвейер коллектора, принимающий независимые короткоживущие дельта-последовательности.
  Процессор delta-to-cumulative с состоянием может прочитать каждый запрос как сброс.

Явный кумулятивный `OTEL_EXPORTER_OTLP_METRICS_TEMPORALITY_PREFERENCE` конфликтует с этим
режимом. Если требования не выполнены, метрики остаются выключенными, а диагностика говорит,
почему. Трейсы и логи это не затрагивает ни в одном из случаев.

## 12. Разработка

`open-telemetry/context` оборачивает каждую активацию контекста в debug-scope под `assert()`: это
объект на каждый спан и сообщение о scope, отцепленном не по порядку. При `zend.assertions=1` —
значение по умолчанию для разработки — такие сообщения стоит читать: вся модель владения в этом
пакете как раз про scope'ы, которые не должны пережить открывшую их работу.

В продакшене `zend.assertions=-1` вырезает assert целиком, и вопрос не возникает. Если
разработческий воркер нагружен настолько, что накладные расходы заметны, отключите именно
обёртку:

```dotenv
OTEL_PHP_DEBUG_SCOPES_DISABLED=true
```

Не ставьте это там, где вы хотели бы узнать об утёкшем scope.

Собственная диагностика пакета — упавшая инструментация, scope, закрытый не по порядку, неудачный
экспорт — идёт в канал Monolog `open_telemetry` с ограничением частоты, вместе с выводом самого
SDK:

```yaml
open_telemetry:
    diagnostics:
        enabled: true
        detailed_per_process: 10
        min_interval_seconds: 60.0
```

`diagnostics.enabled: false` заглушает и SDK, а не возвращает его в `error_log()`.

## Готовые примеры

- [`config/examples/worker.env`](../../config/examples/worker.env) — общий воркер поверх OTLP/HTTP
- [`config/examples/fpm.env`](../../config/examples/fpm.env) — процесс на запрос
- [`config/examples/grpc.env`](../../config/examples/grpc.env) — OTLP/gRPC
- [`config/examples/split-endpoints.env`](../../config/examples/split-endpoints.env) — свой бэкенд на каждый сигнал
- [`config/examples/worker.yaml`](../../config/examples/worker.yaml) · [`config/examples/fpm.yaml`](../../config/examples/fpm.yaml)
- [`config/example_config.yaml`](../../config/example_config.yaml) — все ключи со значениями по умолчанию

Документация OpenTelemetry: [конфигурация PHP SDK](https://opentelemetry.io/docs/languages/php/sdk/) ·
[конфигурация OTLP-экспортёра](https://opentelemetry.io/docs/languages/sdk-configuration/otlp-exporter/)
